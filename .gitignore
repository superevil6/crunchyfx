# Keep Claude's context/notes out of the repo. Everything else is tracked.
CLAUDE.md
.claude/

# Desktop (Tauri) build artifacts — generated frontend copy, Rust output, Tauri schemas.
dist/
src-tauri/target/
src-tauri/gen/

# Saved project files (produced by the Save project feature — user data, not source).
*.crunchyfx
